async function cargarClases(){try{const r=await fetch('/api/clases');const d=await r.json();if(!r.ok)throw Error(d.error);const tb=document.querySelector('tbody');tb.innerHTML='';(d.data||[]).forEach(x=>{const id=x.id_clase??x.id;const tr=document.createElement('tr');tr.innerHTML=`<td>${x.nombre??x.clase??''}</td><td>${x.entrenador??x.id_entrenador??''}</td><td>${x.dia??x.fecha??''}</td><td>${x.horario??x.hora??''}</td><td>${x.cupos_disponibles??x.disponibles??''}</td><td><button class="reservar" data-clase="${id}">Reservar</button></td>`;tb.appendChild(tr);});tb.addEventListener('click',reservar);}catch(e){}}
async function reservar(e){if(!e.target.classList.contains('reservar'))return;const r=await fetch('/api/reservas',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id_clase:e.target.dataset.clase})});const d=await r.json().catch(()=>({}));document.getElementById('reservaMessage').textContent=r.ok?'Reserva realizada.':(d.error||'No se pudo reservar.');}
document.addEventListener('DOMContentLoaded',cargarClases);
