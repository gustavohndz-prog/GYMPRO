document.addEventListener('DOMContentLoaded',async()=>{try{const r=await fetch('/api/cliente/me');const d=await r.json();if(!r.ok)throw Error(d.error);const c=d.data;const card=document.querySelector('.card');if(card){const h=card.querySelector('h3');if(h)h.textContent=c.membresia||'Sin membresía';const ps=card.querySelectorAll('p');ps.forEach(p=>{if(p.textContent.includes('Estado'))p.textContent='Estado: '+(c.estado||'');if(p.textContent.includes('Vencimiento'))p.textContent='Vencimiento: '+(c.fecha_fin||'—');});}}catch(e){}});
