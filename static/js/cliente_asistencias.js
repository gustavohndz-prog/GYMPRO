async function cargarAsistencias(){try{const r=await fetch('/api/cliente/asistencias');const d=await r.json();if(!r.ok)throw Error(d.error);const tb=document.querySelector('tbody');if(!tb)return;tb.innerHTML='';(d.data||[]).forEach(x=>{const tr=document.createElement('tr');tr.innerHTML=`<td>${x.fecha??x.fecha_asistencia??''}</td><td>${x.actividad??x.clase??x.id_clase??''}</td><td>${x.horario??''}</td><td>${x.estado??'Asistió'}</td>`;tb.appendChild(tr);});if(!tb.children.length)tb.innerHTML='<tr><td colspan="4">Sin asistencias registradas.</td></tr>';}catch(e){}}
document.addEventListener('DOMContentLoaded',cargarAsistencias);document.getElementById('filtrarAsistencia')?.addEventListener('click',cargarAsistencias);
