document.addEventListener('DOMContentLoaded',async()=>{try{const r=await fetch('/api/cliente/me');const d=await r.json();if(!r.ok)throw Error(d.error);const c=d.data;document.querySelectorAll('article.card').forEach(a=>{const h=a.querySelector('h3')?.textContent||'';if(h.includes('membresía'))a.querySelector('strong').textContent=c.membresia||'Sin membresía';if(h.includes('Vencimiento'))a.querySelector('strong').textContent=c.fecha_fin||'—';});}catch(e){}});
