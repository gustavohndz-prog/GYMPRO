const $=id=>document.getElementById(id);
const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
function fechaParams(){const p=new URLSearchParams();if($('desde').value)p.set('desde',$('desde').value);if($('hasta').value)p.set('hasta',$('hasta').value);return p.toString();}
function canvasClear(c){const x=c.getContext('2d');const r=c.getBoundingClientRect();const d=window.devicePixelRatio||1;c.width=Math.max(300,r.width*d);c.height=270*d;x.setTransform(d,0,0,d,0,0);x.clearRect(0,0,r.width,270);return [x,r.width,270];}
function barras(id,data,labelKey,valueKey){const c=$(id);if(!c)return;const [x,w,h]=canvasClear(c);const a=(data||[]).slice(0,10);if(!a.length){x.fillStyle='#8C98B3';x.fillText('Sin datos para mostrar',20,40);return;}const max=Math.max(...a.map(v=>Number(v[valueKey])||0),1);const left=42,bottom=45,top=18,gap=8;const bw=Math.max(12,(w-left-15-gap*(a.length-1))/a.length);x.strokeStyle='rgba(255,255,255,.12)';x.beginPath();x.moveTo(left,top);x.lineTo(left,h-bottom);x.lineTo(w-10,h-bottom);x.stroke();a.forEach((v,i)=>{const val=Number(v[valueKey])||0;const bh=(h-bottom-top)*(val/max);const xx=left+i*(bw+gap);const yy=h-bottom-bh;x.fillStyle='#00B4FF';x.fillRect(xx,yy,bw,bh);x.fillStyle='#F4F7FB';x.font='12px system-ui';x.textAlign='center';x.fillText(val.toLocaleString('es-MX'),xx+bw/2,Math.max(12,yy-6));x.fillStyle='#8C98B3';const txt=String(v[labelKey]??'');x.fillText(txt.length>13?txt.slice(0,12)+'…':txt,xx+bw/2,h-20);});}
function linea(id,data,labelKey,valueKey){const c=$(id);if(!c)return;const [x,w,h]=canvasClear(c);const a=data||[];if(!a.length){x.fillStyle='#8C98B3';x.fillText('Sin datos para mostrar',20,40);return;}const vals=a.map(v=>Number(v[valueKey])||0);const max=Math.max(...vals,1),min=Math.min(...vals,0),range=max-min||1,left=42,right=15,top=20,bottom=45; x.strokeStyle='rgba(255,255,255,.12)';x.beginPath();x.moveTo(left,top);x.lineTo(left,h-bottom);x.lineTo(w-right,h-bottom);x.stroke();x.beginPath();vals.forEach((v,i)=>{const px=left+(a.length===1?0:(w-left-right)*i/(a.length-1));const py=top+(h-top-bottom)*(1-(v-min)/range);if(i===0)x.moveTo(px,py);else x.lineTo(px,py);});x.strokeStyle='#00D084';x.lineWidth=3;x.stroke();vals.forEach((v,i)=>{const px=left+(a.length===1?0:(w-left-right)*i/(a.length-1));const py=top+(h-top-bottom)*(1-(v-min)/range);x.fillStyle='#00D084';x.beginPath();x.arc(px,py,4,0,Math.PI*2);x.fill();x.fillStyle='#F4F7FB';x.font='11px system-ui';x.textAlign='center';x.fillText(Number(v).toLocaleString('es-MX',{minimumFractionDigits:2,maximumFractionDigits:2}),px,Math.max(12,py-9));x.fillStyle='#8C98B3';const t=String(a[i][labelKey]??'');x.fillText(t.length>10?t.slice(0,10):t,px,h-20);});}
function tabla(rows){if(!rows?.length)return '<p>Sin datos para el periodo seleccionado.</p>';const keys=Object.keys(rows[0]);return '<table><thead><tr>'+keys.map(k=>`<th>${esc(k.replaceAll('_',' '))}</th>`).join('')+'</tr></thead><tbody>'+rows.map(r=>'<tr>'+keys.map(k=>`<td>${esc(r[k])}</td>`).join('')+'</tr>').join('')+'</tbody></table>';}
async function generar(tipo){$('reportMessage').textContent='Generando reporte...';try{const r=await fetch('/api/reportes/'+encodeURIComponent(tipo)+'?'+fechaParams());const d=await r.json();if(!r.ok)throw new Error(d.detail||d.error||'No se pudo generar el reporte');$('reportMessage').textContent='Reporte generado correctamente.';$('resultadoReporte').innerHTML=tabla(d.data||[]);if(tipo==='ventas'){linea('graficaVentas',d.graficas?.ventas_por_dia||[],'periodo','total');barras('graficaProductos',d.graficas?.productos_mas_vendidos||[],'producto','unidades');}else if(tipo==='inventario'){barras('graficaProductos',d.graficas?.stock_por_producto||[],'producto','stock');}else if(tipo==='membresias'){barras('graficaMembresias',d.graficas?.membresias_por_tipo||[],'membresia','cantidad');barras('graficaEstados',d.graficas?.socios_por_estado||[],'estado','cantidad');}}catch(e){$('reportMessage').textContent='Error: '+e.message;}}
document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('.generar').forEach(b=>b.addEventListener('click',()=>generar(b.dataset.tipo)));$('generarTodo').addEventListener('click',()=>generar('ventas'));$('imprimirReporte').addEventListener('click',()=>window.print());generar('ventas');window.addEventListener('resize',()=>{if(window.__reportData){} });});
